// provide the auth context to the app here
// handle some auth logic
// a good practice is to create a custom hook that provides the values stored in the auth context
// and use that hook in the components that need to access the auth context
// this way, you can easily mock the auth context in tests